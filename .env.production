# Прод-конфіг для збірки (vite build). Значення НЕ секретне — worker URL і так
# потрапляє у клієнтський бандл. Тримаємо тут, щоб не чіпати .github/workflows/
# (зміна workflow-файлів через OAuth-токен потребує окремого `workflow` scope).
VITE_ORDER_API_URL="https://katering-orders.tilandiya.workers.dev"
