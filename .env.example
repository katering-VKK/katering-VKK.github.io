# API для адмінки. GitHub Secret: VITE_ADMIN_API_URL
VITE_ADMIN_API_URL="https://lumu-pearl.vercel.app/api"

# Endpoint прийому замовлень — Cloudflare Worker (див. worker/README.md).
# GitHub Secret: VITE_ORDER_API_URL
VITE_ORDER_API_URL="https://katering-orders.<subdomain>.workers.dev"

# --- Тільки на стороні Cloudflare Worker (wrangler secret put), НЕ комітити ---
# TELEGRAM_BOT_TOKEN  — токен з BotFather (НОВИЙ, після /revoke)
# TELEGRAM_CHAT_ID    — numeric chat id
